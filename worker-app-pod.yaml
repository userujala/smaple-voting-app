apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: worker-app-pod
    app: sample-voting-app
spec:
  containers:
    - name: worker-app
      image: ujalasap/worker-app:v1
      